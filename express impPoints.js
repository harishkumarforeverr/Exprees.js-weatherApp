// (req, res) =>
// in req we have

// req.params === https://localhost:9000/harishhshhshshhs  here harishhshhshshhs is parms
// example   const { id } = req.params;

// req.body  === is the body send by the clinet

// req.query  === here https://localhost:9000/users?name=harish&age=21
// here query is  object and like these { name:harish, age 21}
